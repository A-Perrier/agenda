const initialState = {
  events: []
}

export function manageEvents (state = initialState, action) {
  let nextState

  switch (action.type) {
    case '':
      
      break;
  
    default:
      return state
      break;
  }
}